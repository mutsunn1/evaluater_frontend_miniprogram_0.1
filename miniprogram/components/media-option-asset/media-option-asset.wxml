<!-- Option media asset thumbnail — image or audio icon -->
<image wx:if="{{isImage}}" class="moa-img" src="{{asset.url}}" mode="aspectFill" />
<view wx:elif="{{isAudio}}" class="moa-audio-icon">🔊</view>
